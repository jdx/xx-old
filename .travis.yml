language: rust
rust:
  - stable
  - nightly
  - beta:
      before_script:
        - rustup component add clippy-preview
      script:
        - cargo clippy -- -D warnings
        - cargo test
matrix:
  allow_failures:
    - rust: nightly
