language: rust
cache: cargo
matrix:
  allow_failures:
    - rust: nightly
  include:
    - rust: stable
  include:
    - rust: nightly
  include:
    - rust: beta
      before_script:
        - rustup component add clippy-preview
      script:
        - cargo clippy -- -D warnings
        - cargo test
